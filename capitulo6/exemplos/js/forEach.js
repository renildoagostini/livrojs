const cidades = ["Vitória", "Serra", "Fundão"];
    cidades.forEach((cidade, i) =>{
        console.log(`${i + 1}ª Cidade: ${cidade}`);
    });

const numeros = [5, 10, 15, 20];
let soma = 0;

numeros.forEach(numeros => soma += numeros)
console.log(`Soma dos números: ${soma}`);