const cidades = ["Vitória", "Serra", "Fundão"];
    for(let i = 0; i < cidades.length; i++){
        console.log(cidades[i]);
    }

    console.log("*".repeat(40)); //repete 40 vezes o *
    console.log(cidades.toString()); //coloca uma virgula entre os elementos do vetor
    console.log(cidades.join(" - ")); //coloca qualquer simbolo entre os elementos do vetor